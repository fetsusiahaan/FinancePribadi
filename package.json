{
  "name": "finance-pribadi",
  "private": true,
  "workspaces": [
    "backend",
    "frontend"
  ],
  "scripts": {
    "kill-ports": "npx kill-port 5173 5183 || exit 0",
    "dev": "npm run kill-ports && concurrently -n backend,frontend -c blue,green \"npm run dev -w backend\" \"node server.js\"",
    "start": "npm run build -w frontend && npm run kill-ports && concurrently -n backend,frontend -c blue,green \"npm run start -w backend\" \"node server.js\""
  },
  "devDependencies": {
    "concurrently": "^9.0.1"
  },
  "dependencies": {
    "express": "^5.2.1"
  }
}
