node_modules/
dist/
build/
frontend/dist/

.env
.env.*
!.env.example

logs/
npm-debug.log*

# Dump database (backend/scripts/backup-db.ps1). Isinya SELURUH data user —
# email, hash password, transaksi, foto profil. Tidak boleh ikut ter-commit.
_migration/
*.sql.bak
backup-*.sql

# Android RN app — tidak ikut di-commit/push
android-apk/

.DS_Store
.vscode/

# Skill Claude Code pihak ketiga — di-install per mesin, bukan bagian source project.
.claude/
